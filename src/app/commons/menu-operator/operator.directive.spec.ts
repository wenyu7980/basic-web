import { OperatorDirective } from './operator.directive';

describe('OperatorDirective', () => {
  it('should create an instance', () => {
    const directive = new OperatorDirective();
    expect(directive).toBeTruthy();
  });
});
